<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="buyerns">

	<select id="list" resultType="buyer">
		select * from m_buyer
	</select>
	<insert id="insert" parameterType="buyer">
		insert into m_buyer values (#{buyerCd}, #{countryCd},#{bname},#{manager},#{address},#{tel},#{email},sysdate,'N', null)
	</insert>
	<update id="delete" parameterType="String">
		update m_buyer set del='Y', statusdate = sysdate where buyer_cd=#{buyerCd}	
	</update>
	
</mapper>